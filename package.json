{
  "name": "redwood-starter",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite dev",
    "dev:init": "rw-scripts dev-init",
    "build": "vite build",
    "dev:test:render": "bun scripts/test-render.ts",
    "clean": "bun run clean:vite",
    "clean:vite": "rm -rf ./node_modules/.vite",
    "cf-types": "bunx wrangler types",
    "release": "rw-scripts ensure-deploy-env && bun run clean && RWSDK_DEPLOY=1 bun run build && wrangler deploy",
    "prepare": "vibe-rules install unified"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.8",
    "@types/bun": "latest",
    "@types/react": "^19.1.7",
    "@types/react-dom": "^19.1.6",
    "drizzle-kit": "^0.31.4",
    "vibe-rules": "^0.3.85",
    "wrangler": "^4.33.0"
  },
  "dependencies": {
    "@ai-sdk/openai": "^2.0.16",
    "@cloudflare/puppeteer": "^1.0.4",
    "ai": "^5.0.17",
    "drizzle-orm": "^0.44.4",
    "rwsdk": "^0.1.36",
    "vite": "^6.3.5",
    "zod": "^4.0.17"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
